// Average resampling converts the captured mono stream to 16 kHz PCM for Whisper.
export function encodeWav(input,rate){const ratio=rate/16000,n=Math.floor(input.length/ratio),buf=new ArrayBuffer(44+n*2),view=new DataView(buf);const word=(offset,s)=>{for(let i=0;i<s.length;i++)view.setUint8(offset+i,s.charCodeAt(i));};word(0,'RIFF');view.setUint32(4,36+n*2,true);word(8,'WAVE');word(12,'fmt ');view.setUint32(16,16,true);view.setUint16(20,1,true);view.setUint16(22,1,true);view.setUint32(24,16000,true);view.setUint32(28,32000,true);view.setUint16(32,2,true);view.setUint16(34,16,true);word(36,'data');view.setUint32(40,n*2,true);for(let i=0;i<n;i++){let sum=0,count=0;for(let j=Math.floor(i*ratio);j<Math.floor((i+1)*ratio)&&j<input.length;j++){sum+=input[j];count++;}const v=Math.max(-1,Math.min(1,sum/(count||1)));view.setInt16(44+i*2,v<0?v*32768:v*32767,true);}return buf;}
